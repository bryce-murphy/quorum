# Schemas

JSON Schema for every Quorum structured artifact (task manifest, handoff, review/finding,
claim, decision, retro). **Placeholder** - the schemas are defined by the architecture
spec session (`docs/SPEC-HANDOFF.md`, task 3) and land as ADR-0002+ is implemented.
Per principle 6, every artifact is machine-first (schema-validated) with a rendered
Markdown view.
